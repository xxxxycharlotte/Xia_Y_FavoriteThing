const config = {
    user: "root",
    pword: "", // windows users leave this blank
    host: "localhost",
    port: "3306", // windows users - sql port is 3306
    database: "waltdisney"
};

module.exports = config;
